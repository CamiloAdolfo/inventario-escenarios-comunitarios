specVersion: "v1beta"
serviceId: "inventario-escenarios"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "inventario-escenarios-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
